## User Story

As a prospective Fix-It customer, I want to complete email capture, booking details, and payment directly in a guided on-site booking flow powered by Google Calendar availability, so that I can secure a session without leaving the site and receive immediate confirmation after successful payment.

## Tasks

|  ID | Title                                                 | Description                                                                                                                                                                   | Acceptance Criteria                                                                                                                                                                                                                                                                                      | User Verification                                                                                                                                                                                                    | Status   |
| --: | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
|   1 | Route booking into app                                | Booking CTA takes users to `/booking`, where the page renders the 3-section vertical layout optimized for mobile.                                                             | - Booking CTA no longer redirects to Google Calendar (AC1).<br>- `/booking` renders Email, Booking date/time, Payment in vertical order on mobile widths (AC2).                                                                                                                                          | - From homepage, click booking CTA and confirm URL is `/booking`.<br>- Resize to mobile viewport and confirm all 3 sections are vertically stacked and readable.                                                     | Complete |
|   2 | Gate progression by validated form state              | Booking form progressively reveals section 2 after valid email and section 3 after valid date/time, with fade-in transitions and disabled payment until all fields are valid. | - Section 2 is hidden until section 1 validates, then fades in (AC3).<br>- Section 3 is hidden until section 2 validates, then fades in (AC4).<br>- Payment submit stays disabled until all required fields are valid (AC10).                                                                            | - Enter invalid then valid email and confirm section 2 reveal behavior.<br>- Pick valid date/time and confirm section 3 reveal behavior.<br>- Attempt payment submit with incomplete form and confirm blocked state. | Complete |
|   3 | Build Sendy mock enrollment API endpoint              | Add Next.js `/api` mock endpoint for pre-booking email enrollment and connect section 1 so successful validation invokes this server route.                                   | - Section 1 success calls a Next.js `/api` mock endpoint for add-to-pre-booking (AC5).<br>- Endpoint returns deterministic success/failure contract and logs outcome (NFR observability).<br>- Endpoint contributes to required `/api` route-handler architecture (AC13).                                | - Complete section 1 with valid email and confirm request hits app `/api` route and returns success.<br>- (Dev) Check logs for enrollment event and status.                                                          | Complete |
|   4 | Build Google availability mock API endpoint           | Add Next.js `/api` mock endpoint that returns Google-schedule-shaped availability for section 2 in `Australia/Sydney`, then consume it in the booking UI.                     | - Section 2 availability is fetched through app `/api` route backed by Google-schedule contract data (AC6).<br>- Returned options map to Sydney-time slots only: 9:00am, 10:15am, 11:30am, 12:45pm when available (AC7).<br>- Endpoint contributes to required `/api` route-handler architecture (AC13). | - Load `/booking` and verify section 2 issues request to app `/api` availability endpoint.<br>- Confirm visible slots align to configured Sydney slot windows from response data.                                    | Complete |
|   5 | Enforce slot validity and provisional scheduling      | Booking selection only accepts available slot responses, blocks invalid/unavailable picks, and keeps scheduling provisional before payment.                                   | - Invalid/unavailable slot selection is blocked with clear feedback (AC8).<br>- Selecting a slot does not create/finalize booking before payment success (AC9).                                                                                                                                          | - Attempt selecting an unavailable/non-slot value and confirm progression is blocked with message.<br>- Inspect network calls and confirm no finalization call occurs before payment.                                | Complete |
|   6 | Build Stripe mock payment API endpoint                | Add Next.js `/api` mock endpoint for payment completion and connect section 3 submit so successful completion triggers post-payment flow transitions.                         | - Payment completion uses app `/api` route with deterministic Stripe-shaped mock response.<br>- Payment handler logs outcomes (NFR observability).<br>- Endpoint contributes to required `/api` route-handler architecture (AC13).                                                                       | - Submit valid payment form and verify app `/api` payment route responds success.<br>- Confirm payment response includes next-step state required for transition handling.                                           | Complete |
|   7 | Build mock post-payment flow-transition API endpoint  | Add Next.js `/api` mock endpoint for post-payment transitions that removes pre-booking and adds booked flow state, and invoke it only after successful payment completion.    | - Successful payment triggers flow-transition `/api` route and performs remove-from-pre-booking and add-to-booked behavior (AC11).<br>- Flow-transition operation is implemented as app `/api` route handler (AC13).<br>- Transition handler logs outcomes (NFR observability).                          | - Complete mocked payment and verify flow-transition `/api` call executes successfully.<br>- Confirm both remove and add transition outcomes are returned/logged.                                                    | Complete |
|   8 | Complete success redirect and token confirmation view | After successful payment mock, redirect to `/success` with `bookingToken` and show a confirmation state tied to token presence.                                               | - Redirect lands on `/success` with non-empty `bookingToken` query value (AC12).<br>- Success page renders confirmation state when token is present.                                                                                                                                                     | - Complete flow and verify browser URL contains `bookingToken` on `/success`.<br>- Confirm success page shows confirmation content for tokenized completion.                                                         | Complete |

## Notes

- Task: JD-001
- Title: Booking Form
- Created: 2026-02-26
