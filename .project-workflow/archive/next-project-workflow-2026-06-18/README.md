# Archived Next Project Workflow

This is the preserved snapshot of `next/.project-workflow/` before project-workflow was removed from the `next` repository on 2026-06-18.

Use this archive for historical requirements, implementation notes, acceptance audits, and old tracker state. Do not update it as live workflow state.

For new work, create or update tasks from the workspace root:

```sh
./.project-workflow/cli/workflow
```

When a new parent task continues one of these archived items, link back to the relevant archived task document and record `Primary repo`, `Repos touched`, and branch/PR state in the new parent task.
